backend:
  name: git-gateway
  branch: react_app    # Branch, in dem die Änderungen gespeichert werden

media_folder: "public/images"    # Ordner, in dem Bilder gespeichert werden
public_folder: "/images"         # Pfad, wie du im Code auf die Bilder zugreifst

collections:
  - name: "config"
    label: "Konfiguration"
    delete: false
    editor:
      preview: false
    files:
      - file: "public/config.json"
        label: "Website-Konfiguration"
        name: "site_config"
        fields:
          - { label: "Über mich", name: "about_me", widget: "text" }
          - label: "Projekte"
            name: "projects"
            widget: "list"
            fields:
              - { label: "Titel", name: "title", widget: "string" }
              - { label: "Beschreibung", name: "description", widget: "text" }
              - { label: "Bilder", name: "pictures", widget: "list", field: { label: "Bild", name: "image", widget: "image" } }
              - { label: "Hintergrundfarbe", name: "background_color", widget: "string" }
